#!/bin/bash
echo "Enter a number:"
read num
rev=$(echo $num | rev)
[ "$num" = "$rev" ] && echo "$num is a Palindrome" || echo "$num is not a Palindrome"


# Output:
Enter a number:
121
121 is a Palindrome
